import {defineMessages} from 'react-intl';

const messages = defineMessages({
    title: {
        id: 'containers.home.title',
        defaultMessage: 'Single job post',
    },
});

export default messages;
