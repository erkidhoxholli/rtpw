import { defineMessages } from 'react-intl';

const messages = defineMessages({
    title: {
        id: 'containers.listUsers.title',
        defaultMessage: 'All users',
    },
});

export default messages;
