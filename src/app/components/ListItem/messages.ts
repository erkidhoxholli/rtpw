import { defineMessages } from 'react-intl';

const messages = defineMessages({
    noDescription: {
        id: 'components.repos.listItem.noDescription',
        defaultMessage: 'No description found.',
    },
});

export default messages;
