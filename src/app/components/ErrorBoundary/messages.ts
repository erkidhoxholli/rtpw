import { defineMessages } from 'react-intl';

const messages = defineMessages({
    generalError: {
        id: 'components.errorBoundary.generalError',
        defaultMessage: 'General error!',
    },
});

export default messages;
